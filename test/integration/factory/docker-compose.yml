services:
  test:
    build:
      context: ../../..
      dockerfile: test/integration/factory/Dockerfile
    volumes:
      - ../../..:/app
    working_dir: /app

networks:
  default:
    name: objstore-factory-test
