services:
  test:
    build:
      context: ../../..
      dockerfile: test/integration/replication/Dockerfile
    volumes:
      - ../../..:/app
    working_dir: /app

networks:
  default:
    name: objstore-replication-test
