services:
  test:
    build:
      context: ../../..
      dockerfile: test/integration/local/Dockerfile
    volumes:
      - ../../..:/app
    working_dir: /app

networks:
  default:
    name: objstore-local-test
